/**
 * Local configuration template. Copy this file to src/config.ts and fill in
 * your credentials from the Google Cloud Console.
 *
 * src/config.ts is gitignored — never commit real credentials.
 *
 * CLIENT_ID: OAuth 2.0 Client ID (looks like <numbers>-<hash>.apps.googleusercontent.com)
 * API_KEY:   Browser API key (used to call the Tasks REST API without OAuth for public data;
 *            in this app it accompanies the OAuth client for initialisation)
 */
export const CLIENT_ID = "YOUR_CLIENT_ID.apps.googleusercontent.com";
export const API_KEY = "YOUR_API_KEY";
